# Filas-FIFO-First-in-First-Out-

Projeto em Java/Maven para estudo de fila (FIFO - First In, First Out).

## Como executar

```bash
mvn -f fila/pom.xml clean package
java -cp fila/target/classes org.estudos.Main
```

